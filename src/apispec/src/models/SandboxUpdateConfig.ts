/* tslint:disable */
/* eslint-disable */
/**
 * Sandbox0 API
 * Public HTTP APIs exposed by Sandbox0 regional gateways and the global gateway.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SandboxAppService } from './SandboxAppService';
import {
    SandboxAppServiceFromJSON,
    SandboxAppServiceFromJSONTyped,
    SandboxAppServiceToJSON,
    SandboxAppServiceToJSONTyped,
} from './SandboxAppService';

/**
 * Durable lifecycle and service fields that can be updated without replacing
 * the current runtime allocation. Network policy uses the dedicated network
 * endpoint. Environment, resource, and webhook changes require a new runtime.
 * 
 * @export
 * @interface SandboxUpdateConfig
 */
export interface SandboxUpdateConfig {
    /**
     * Runtime soft time-to-live in seconds. When it expires, Sandbox0 checkpoints the writable rootfs, pauses the sandbox, and releases runtime compute while preserving durable sandbox state.
     * @type {number}
     * @memberof SandboxUpdateConfig
     */
    ttl?: number;
    /**
     * Sandbox hard time-to-live in seconds. When it expires, Sandbox0 deletes the sandbox identity and durable state, including paused rootfs checkpoints.
     * @type {number}
     * @memberof SandboxUpdateConfig
     */
    hardTtl?: number;
    /**
     * Controls whether supported inbound API or public exposure requests may automatically
     * make an inactive sandbox available. This setting does not control platform-initiated
     * runtime fault recovery. A supported access request returns `503 unavailable` with
     * `sandbox is waking up` when an accepted resume has not committed yet. It returns
     * `503 sandbox_resume_failed` when that resume attempt has ended unsuccessfully.
     * 
     * @type {boolean}
     * @memberof SandboxUpdateConfig
     */
    autoResume?: boolean;
    /**
     * 
     * @type {Array<SandboxAppService>}
     * @memberof SandboxUpdateConfig
     */
    services?: Array<SandboxAppService>;
}

/**
 * Check if a given object implements the SandboxUpdateConfig interface.
 */
export function instanceOfSandboxUpdateConfig(value: object): value is SandboxUpdateConfig {
    return true;
}

export function SandboxUpdateConfigFromJSON(json: any): SandboxUpdateConfig {
    return SandboxUpdateConfigFromJSONTyped(json, false);
}

export function SandboxUpdateConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): SandboxUpdateConfig {
    if (json == null) {
        return json;
    }
    return {
        
        'ttl': json['ttl'] == null ? undefined : json['ttl'],
        'hardTtl': json['hard_ttl'] == null ? undefined : json['hard_ttl'],
        'autoResume': json['auto_resume'] == null ? undefined : json['auto_resume'],
        'services': json['services'] == null ? undefined : ((json['services'] as Array<any>).map(SandboxAppServiceFromJSON)),
    };
}

export function SandboxUpdateConfigToJSON(json: any): SandboxUpdateConfig {
    return SandboxUpdateConfigToJSONTyped(json, false);
}

export function SandboxUpdateConfigToJSONTyped(value?: SandboxUpdateConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'ttl': value['ttl'],
        'hard_ttl': value['hardTtl'],
        'auto_resume': value['autoResume'],
        'services': value['services'] == null ? undefined : ((value['services'] as Array<any>).map(SandboxAppServiceToJSON)),
    };
}

